{
  "name": "documents-create",
  "description": "Create a new document in Shortcut with a title and content. Returns the document's id, title, and app_url. Note: Use Markdown format for the content.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "title": {
        "type": "string",
        "maxLength": 256,
        "description": "The title for the new document (max 256 characters)"
      },
      "content": {
        "type": "string",
        "description": "The content for the new document in Markdown format."
      }
    },
    "required": ["title", "content"]
  }
}
